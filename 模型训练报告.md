# VOCs 模型训练报告

## 1. 报告目标

本报告说明当前版本完整训练与验证链路，重点覆盖：

- 数据预处理流程
- 模型结构设计
- 关键参数配置
- Trend Accuracy（trend acc）验证结果

预测任务定义：基于历史 24 小时数据（96 步，15min/步），预测未来 6 小时 VOCs 浓度（24 步）。

---

## 2. 数据预处理流程

数据处理由训练脚本中的处理器统一执行，流程如下。

### 2.1 原始数据读取

- 数据文件：`src/data/vocs_dataset.csv`
- 目标列：`rto_out_conc`

### 2.2 时间周期特征

为增强模型对日周期与周周期的感知，构造周期编码特征：

- `hour_sin`, `hour_cos`
- `weekday_sin`, `weekday_cos`
- `month_sin`, `month_cos`

### 2.3 统计与差分特征

围绕目标及关键协变量构造滚动和差分特征：

- Rolling 均值/标准差/趋势（窗口 6/12/24/48/96）
- 差分特征：`diff_1`, `diff_4`, `diff_24`, `diff_96`
- 均值偏离：`ma_diff_24`

### 2.4 显式趋势增强

为加强趋势方向学习能力，对目标列与全部 23 个核心协变量统一注入显式一阶与二阶变化信息：

- 覆盖列：`rto_out_conc` + 环境/工艺协变量（`ambient_temp`、`coating_flow`、`rto_in_temp` 等共 23 列）
- 每列各生成两个差分特征：`{col}_diff_1`（一阶差分，刻画瞬时变化速率）、`{col}_diff_2`（二阶差分，刻画变化加速度）
- 合计新增约 48 个趋势特征，显著提升输入空间中趋势信号的密度

### 2.5 超标样本增强

对超过阈值（80）的样本进行轻噪声增强，提高模型在高值区间与临界区间的鲁棒性。

### 2.6 标准化与序列化

- 输入标准化：`feature_scaler`
- 目标标准化：`target_scaler`
- 历史窗口：`SEQ_LEN=96`
- 预测窗口：`PRED_LEN=24`

序列构造时，将历史目标拼接为输入的一部分，以提升自回归预测稳定性。

### 2.7 数据集划分

- 训练集：70%
- 验证集：15%
- 测试集：15%

---

## 3. 模型结构设计

采用 Seq2Seq + Attention + Gate 主干

### 3.1 编码器

- 多层 LSTM 编码历史序列
- 输出隐藏状态供解码器与注意力模块使用

### 3.2 解码器

- 多层 LSTM 自回归逐步生成未来 24 步
- 与编码端信息进行跨步交互

### 3.3 注意力机制

- 使用改进注意力模块聚焦历史关键时刻
- 引入位置相关偏置，提升长时依赖建模能力

### 3.4 门控融合

- 将 decoder 当步输出与 attention context 拼接后送入线性层，经 sigmoid 激活得到门控权重 `g`
- 融合输出为 `g ⊙ h_dec + (1−g) ⊙ context`，使模型在注意力主导（突变段）与解码器主导（平稳段）之间连续自适应切换
- 门控权重完全由数据驱动，无需手工区分平稳/突变区间

---

## 4. 参数配置

- `SEQ_LEN`: 96
- `PRED_LEN`: 24
- `HIDDEN_DIM`: 256
- `NUM_LAYERS`: 3
- `DROPOUT`: 0.3
- `BATCH_SIZE`: 64
- `LEARNING_RATE`: 0.0008
- `WEIGHT_DECAY`: 1e-5
- `EPOCHS`: 150
- `EARLY_STOP_PATIENCE`: 20
- `GRAD_CLIP`: 1.0
- `TEACHER_FORCING`: 0.5 -> 0.05（衰减系数 0.98）
- `EXCEED_THRESHOLD`: 80
- `LOSS_WEIGHTS`: short=3.0, medium=2.0, long=1.0

### 4.1 损失函数

当前版本采用“数值误差 + 趋势约束”的组合损失：

- 基础项：分阶段加权 MSE（short/medium/long）
- 趋势幅度项：相邻步差分的 MSE（约束变化幅度）
- 趋势方向项：方向不一致惩罚（约束升降方向）

组合形式可概括为：

- `L_total = 1.0 * L_base + 0.5 * L_diff + 0.1 * L_dir`

其中 `L_base` 仍保留超标样本加权机制（`EXCEED_THRESHOLD=80`）。

### 4.2 学习率策略

- 初始学习率：`LEARNING_RATE = 0.0008`
- 调度器：`ReduceLROnPlateau`
- 调度模式：`mode=max`，`factor=0.5`，`patience=10`

学习率下降依据为趋势优先复合指标：

- `scheduler_metric = trend_acc + 0.05 * val_r2`

即优先保证趋势方向准确率，在趋势相近时再由 `val_r2` 提供辅助区分。

---

## 5. Trend Acc 验证

### 5.1 验证口径

- trend_acc(预测准确率)

### 5.2 指标结果

从最新对比结果中，对应指标为：

- `trend_acc = 0.7607`
- `avg_r2 = 0.6336`
- `exceed_acc = 0.9568`

### 5.3 与其他版本的 trend acc 对比（同口径）

| 版本 | trend_acc |
|---|---:|
| v2 | 0.6222 |
| v3 | 0.6281 |
| v4 | 0.7607 |
| v5 | 0.7607 |
| v6 | 0.7617 |

结论：

- 最终模型的 trend acc 达到要求，较v4/v5增加0.1%。
- 在当前口径下，v6 相比 v2 有明显提升。
- v6 在趋势方向稳定性上已达到可用水平，可作为当前主版本继续迭代。

## 6. 创新点

### 6.1 v3：线性旁路与混合预测（后续废弃）

在 v2 基础上，v3 尝试引入线性旁路架构，采用混合 LSTM + AR 预测方式：

- 主干仍为 LSTM 时序建模
- 新增 AR 线性分支刻画短期线性变化
- 最终输出由 LSTM 分支与 AR 分支融合得到

实验结果显示该方案无明显提升，因此该方向在后续版本中被废弃。

### 6.2 v4：趋势准确率优先（损失函数改造）

v4 将 trend acc 的优化目标前置，核心改动为把趋势目标显式加入训练损失并参与反向传播：

- 将趋势方向正确性作为训练阶段的重点目标之一
- 训练目标不再只关注数值误差，也强调趋势一致性
- 为后续版本提供统一的优化方向与评估优先级

该代的重点是把“评测指标”转化为“训练目标”。

### 6.3 v5：在 v4 思路上的调参强化

在 v4 已确立的“趋势优先”方向上做参数与训练策略优化：

- 围绕既有目标进行权重与训练参数微调
- 在不大改结构的前提下提升收敛稳定性与泛化表现
- 持续增强趋势方向预测的一致性

### 6.4 v6：趋势特征注入（输入维度扩展）

v6 在输入端做趋势特征注入，将拟合的一阶、二阶差分作为新增输入维度：

- 新增一二阶差分特征（速度、加速度）
- 将上述特征并入模型输入，增强趋势可辨识性

通过特征显式化提升趋势信息密度。

本轮结果中，v6 trend acc = 0.7617，符合要求。

### 6.5 逐代演进总结

- v3：引入线性旁路与 LSTM+AR 混合预测，但无明显提升并废弃
- v4：提出并确立“趋势优先”的优化思路
- v5：沿用 v4 思路并做参数调优强化
- v6：注入一阶/二阶差分特征，扩展输入维度