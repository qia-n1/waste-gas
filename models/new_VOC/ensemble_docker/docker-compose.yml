version: "3.8"
services:
  voc-ensemble-api:
    build: .
    container_name: voc_ensemble_service
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 4G
