# 模型端与后端交互协议（固定 v6 部署）

## 1. 目的与范围

本文用于后端对接当前 VOCS 模型服务。

当前版本约束：
- 推理模块内部固定为 v6 工件。
- 输入输出契约固定，不允许运行时切换到其他模型族。
- 未满足 v6 就绪条件时，接口返回明确错误，不做静默回退。

适用服务入口：
- [vocs_server.py](vocs_server.py)
- [src/inference_module.py](src/inference_module.py)

## 2. 对接总览

后端与模型服务交互分两类：

1) 批量预测接口（推荐主链路）
- 路径：POST /predict
- 作用：一次提交 96 步历史序列，返回未来 24 步预测与归因结果。

2) 流式上报接口（兼容链路）
- 路径：POST /sensor-data
- 作用：每次上报单点，服务侧累积到窗口后自动触发预测。

运行状态查询：
- 路径：GET /status
- 作用：查看模块是否就绪、模型族、契约版本等。

## 3. 固定部署约束（必须）

### 3.1 固定 v6 工件

服务默认要求：
- MODEL_PATH=models/vocs_seq2seq_v6_best.pth
- SCALER_PATH=models/vocs_scalers_v6.pkl

容器编排默认已固定为 v6：
- [docker-compose.yml](docker-compose.yml)

### 3.2 就绪门禁

服务启动后会校验：
- 模型路径命名包含 v6。
- scaler 路径命名包含 v6。
- 模型文件存在且可加载。
- scaler 文件存在且可加载。

不满足时：
- status 中 module_ready=false。
- /predict 返回 503。

## 4. 批量预测接口（主接口）

### 4.1 请求

方法与路径：
- POST /predict

请求体格式：
- data_sequence 长度必须为 96。
- 每个点包含：
  - timestamp: string
  - feature_values: number[25]

feature_values 顺序必须严格与下表一致：

1. ambient_temp
2. ambient_humidity
3. ambient_pressure
4. coating_flow
5. coating_conc
6. coating_temp
7. coating_pressure
8. rotor_speed
9. adsorption_fan_power
10. desorption_fan_power
11. rotor_inlet_temp
12. rotor_inlet_humid
13. desorption_temp
14. concentrated_flow
15. concentrated_conc
16. concentrated_temp
17. concentrated_pressure
18. rto_in_flow
19. rto_in_temp
20. rto_in_pressure
21. burner_gas_flow
22. combustion_temp
23. rto_in_conc
24. rto_out_conc
25. rto_out_temp

请求示例：

    {
      "data_sequence": [
        {
          "timestamp": "2026-04-18T10:00:00",
          "feature_values": [21.5, 63.5, 101.3, 8500.0, 45.0, 26.0, 101.2, 4.7, 44.0, 3.5, 26.0, 65.0, 205.0, 850.0, 400.0, 185.0, 101.6, 1000.0, 185.0, 101.5, 11.0, 757.8, 300.0, 45.0, 62.0]
        }
      ]
    }

说明：示例中 data_sequence 仅展示 1 项，实际必须传 96 项。

### 4.2 成功响应

状态码：200

响应体结构：
- status: success
- predictions: 长度 24
- is_exceed_warning: bool
- alerts: 告警列表
- incremental_attribution:
  - baseline
  - target
  - total_increment
  - feature_contributions
  - group_contributions
  - heatmap

heatmap 约束：
- time_steps 长度固定 24（t+1 到 t+24）
- feature_groups 固定 3 组：
  - 废气源与环境组
  - 转轮浓缩系统
  - RTO焚烧系统
- contribution_matrix 维度固定 24 x 3，且非空

### 4.3 错误响应

1) 400 参数错误
- data_sequence 长度不是 96
- feature_values 维度不是 25

2) 503 模块未就绪
- v6 工件缺失或加载失败
- 服务会返回 Inference module not ready: ...

3) 500 预测异常
- 运行期非参数类错误

4) 典型就绪失败原因（可在 module_ready_reason 中观察）
- invalid_model_artifact
- invalid_scaler_artifact
- model_not_found
- scaler_not_found
- dim_mismatch
- load_error

## 5. 流式上报接口（兼容）

方法与路径：
- POST /sensor-data

作用：
- 每次上报一个完整传感器点。
- 服务侧保存并缓存。
- 达到窗口后自动触发同一推理模块。

注意：
- 该链路已复用与 /predict 相同的模型模块，不再存在第二套推理实现。

## 6. 状态接口（联调必查）

方法与路径：
- GET /status

重点字段：
- model_loaded
- module_ready
- module_ready_reason
- inference_contract_version
- model_family（应为 v6）
- model_path
- scaler_path

联调前请先确认：
- module_ready=true
- model_family=v6

## 7. 联调步骤（建议）

1. 启动服务。
2. 调 GET /status，确认 module_ready=true。
3. 使用固定顺序拼装 96 步 data_sequence，请求 POST /predict。
4. 校验响应体：
   - predictions 长度 24
   - heatmap 维度 24 x 3
   - feature_groups 为三组固定值
5. 验证超标行为：
   - 若 predictions 存在 >80，is_exceed_warning=true 且 alerts 非空。

## 8. 最小 cURL 示例

说明：以下为调用骨架，payload.json 需自行填充为 96 步数据。

    curl -X POST "http://127.0.0.1:8001/predict" \
      -H "Content-Type: application/json" \
      -d @payload.json

状态检查：

    curl "http://127.0.0.1:8001/status"

## 9. 版本与变更策略

当前契约：
- inference_contract_version: v1-fixed-v6

变更原则：
- 非必要不改输入输出字段。
- 如需升级模型，仅允许同族 v6 工件替换。
- 若将来升级契约版本，必须新增版本号并保证向后兼容策略。
